# Use npm as the package manager
package-manager=npm

# Prevent npm from generating a package-lock.json file
package-lock=false

# Save exact versions in package.json
save-exact=true

# Use the official npm registry
registry=https://registry.npmjs.org/

# Increase network timeout for better reliability
network-timeout=60000